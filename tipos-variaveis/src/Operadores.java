public class Operadores {
    public static void main(String[] args) {
        // String Comeco = "Meu nome é ";
        // String NomeCompleto ="Cláudio " + "Gabriel";
        // String tudo = Comeco + NomeCompleto; // Concatenação de strings
        // System.out.println(tudo); // Saída: CláudioGabriel

        // int a, b;

        // a = 5;
        // b = 5;

        // /* EXEMPLO DE CONDICIONAL UTILIZANDO UMA ESTRUTURA IF/ELSE
        // if(a==b)
        // resultado = "verdadeiro";
        // else
        // resultado = "falso";
        // */

        // //MESMA CONDICIONAL, MAS DESSA VEZ, UTILIZANDO O OPERADOR CONDICIONAL TERNÁRIO
        // String resultado = (a!=b) ? "verdadeiro" : "false";

        // System.out.println(resultado);

        // OPERADORES LÓGICOS
        
        boolean a = true;
        boolean b = false;

        if (a && b) {
            System.out.println("Ambos são verdadeiros.");
        }
        if (b || a) {
            System.out.println("Pelo menos um é verdadeiro.");
            
        }
        System.out.println("fim");
    
    }
}
